
// Primary Tools for Edding
export enum PrimaryTool {
    Select,
    Delete,
    Connect,
    TextEdit,
    AddComputer = "computer",
    AddServer = "server",
    AddRouter = "router",
    AddSwitch = "switch",
    AddCloud = "cloud",
    AddTextBox = "text-box",
    AddBgArea = "bg-area"
}


export enum Direction {
    Up,
    Down,
    Left,
    Right
}